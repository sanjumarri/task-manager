{
  "name": "task-manager",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix server\" \"npm:dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
